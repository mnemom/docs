{
  "_comment": "Synthetic manifest for enforce-docs-gate.sh's self-test only -- pretends `links` is ALSO flipped to blocking, to prove the mechanism generically (independent of today's real, still-dirty links state -- see docs-gate-thresholds.json for the real, currently-committed policy).",
  "layers": {
    "sdk": { "mode": "blocking", "budget": 0 },
    "links": { "mode": "blocking", "budget": 0 },
    "api-examples": { "mode": "advisory" }
  }
}
