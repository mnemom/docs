{
  "_comment": "Allowlist for check-path-references.mjs. Each entry is a NORMALIZED API-path key (leading '/', no '/v1' prefix, params as '{}' or '{name}') that the detector should NOT flag. Only add paths that are intentionally-illustrative or belong to a DIFFERENT surface than the customer REST API — never a real removed/deprecated endpoint (those are findings to surface). Keep a 'reason' on every entry.",
  "allow": [
    {
      "path": "/trust/slos",
      "reason": "AEGIS's public Protection-Network SLO page at trust.mnemom.ai/slos, rendered in docs as a bare `/trust/slos` label. A published transparency surface, not an api.mnemom.ai/v1 REST operation (no such op exists in the spec)."
    },
    {
      "path": "/admin/platform-card/alignment",
      "reason": "Intentional old-path reference in old→new migration table in `concepts/cards-as-resources.mdx`. Canonical replacement: `/alignment/platform/default`. Not a live endpoint."
    },
    {
      "path": "/admin/platform-card/alignment/preview-compose",
      "reason": "Same migration table row as `/admin/platform-card/alignment`. Not a live endpoint."
    },
    {
      "path": "/admin/platform-card/protection",
      "reason": "Same migration table in `concepts/cards-as-resources.mdx`. Canonical replacement: `/protection/platform/default`. Not a live endpoint."
    },
    {
      "path": "/admin/platform-card/protection/preview-compose",
      "reason": "Same migration table row as `/admin/platform-card/protection`. Not a live endpoint."
    },
    {
      "path": "/internal/governance-signals",
      "reason": "Changelog.mdx removal entry (amendment 2026-05-07): fast-path agent-scoped endpoint removed. Intentional removal documentation."
    },
    {
      "path": "/safe-house/sovereign-setup",
      "reason": "Changelog.mdx removal entry (amendment 2026-05-07): sovereign-setup preset endpoint removed. Intentional removal documentation."
    },
    {
      "path": "/agents/{id}/policy",
      "reason": "`policy-management.mdx`, `concepts/policy-engine.mdx`, and `guides/upgrading-to-0-5.mdx` all explicitly state this endpoint was removed in the 2026-04-15 consolidation; policy is now part of the alignment card. Intentional removal documentation across multiple guides."
    },
    {
      "path": "/aip/register",
      "reason": "AIP protocol specification (`protocols/aip/specification.mdx` §10.4) references `POST /v1/aip/register` as the HTTP webhook registration endpoint. The committed spec only carries `/aip/webhooks*` ops. Intentional protocol-spec reference — docs are correct, spec is incomplete."
    },
    {
      "path": "/oauth/device_authorization",
      "reason": "`guides/oauth-device-flow.mdx`. OAuth device flow exists in mnemom-api (MNE-1393) but is absent from committed `openapi.json`. Spec-incomplete, not doc drift. Pending mnemom-api follow-up to add device-flow ops to spec."
    },
    {
      "path": "/oauth/device",
      "reason": "Same guide and reason as `/oauth/device_authorization` above."
    },
    {
      "path": "/agents/{id}/card",
      "reason": "`guides/upgrading-to-0-5.mdx` Note explicitly labels `PATCH /v1/agents/{id}/card` as the old path replaced by `PUT /v1/alignment/agent/{id}` in the 2026-04-15 unified-cards consolidation. Upgrade guide migration note, not active guidance."
    },
    {
      "path": "/agents/{id}/enforcement",
      "reason": "`gateway/enforcement.mdx` and `quickstart/gateway.mdx` explicitly label `PUT /v1/agents/{id}/enforcement` as retired 2026-05-14. Migration-context reference documenting the retired endpoint."
    },
    {
      "path": "/agents/{id}/safe-house/config",
      "reason": "Removed in 2026-04-15 unified-cards consolidation; no canonical replacement. `guides/safe-house-monitoring.mdx` and `guides/safe-house-threat-model.mdx` document the removal for migration context."
    }
  ]
}
