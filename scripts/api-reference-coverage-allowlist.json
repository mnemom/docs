{
  "$comment": "Allowlist for check-api-reference-coverage.mjs (page <-> spec reconciliation auditor). Maps a spec operation key \"METHOD /path\" to the repo-relative .mdx file of a HAND-WRITTEN endpoint page that documents that op but carries NO generated `openapi:` directive (so direction-2 coverage cannot see it structurally). The auditor VERIFIES both that the key is a real op in api-reference/openapi.json AND that the mapped file exists on disk; a stale entry (dead key or missing file) is itself a FAILURE, so this list can never silently rot. ONLY list genuine hand-written pages here. NEVER allowlist a genuine gap to silence the gate — add the endpoint page instead. Today the ONLY two directive-less endpoint pages are the GDPR agent-erasure narratives below (474 pages total, 472 directive-bearing).",
  "allow": {
    "DELETE /agents/{agent_id}": "api-reference/endpoint/delete-agents-agent-id.mdx",
    "GET /agents/{agent_id}/deletion-status": "api-reference/endpoint/get-agents-agent-id-deletion-status.mdx"
  }
}
