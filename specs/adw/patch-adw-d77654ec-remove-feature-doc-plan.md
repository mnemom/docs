# Spec — Patch: Remove out-of-scope feature doc from sync PR

- **Status:** Draft
- **Branch:** chore-issue-232-adw-d77654ec-sync-openapi-freshness
- **Location:** app_docs/feature-d77654ec-sync-openapi-freshness.md
- **Related docs:** /home/runner/work/docs/docs/agents/d77654ec/plan/issue-232-adw-d77654ec-sync-openapi-freshness-plan.md

## Problem / Objective
**Original Spec:** /home/runner/work/docs/docs/agents/d77654ec/plan/issue-232-adw-d77654ec-sync-openapi-freshness-plan.md
**Issue:** `app_docs/feature-d77654ec-sync-openapi-freshness.md` was added by the `sdlc_documenter` agent in commit `f6163d8`. The scope guard for this PR is explicit: only outputs from `node scripts/sync-openapi.mjs` and `node scripts/generate-api-reference.mjs` belong in the diff. This 75-line feature doc is not such an output and must not be in the PR.
**Solution:** Delete `app_docs/feature-d77654ec-sync-openapi-freshness.md` via `git rm` and create a new commit that removes it, replacing the out-of-scope sdlc_documenter commit.

## Approach & Changes
### Files to Modify
- `app_docs/feature-d77654ec-sync-openapi-freshness.md` — delete this file entirely

### Implementation Steps
IMPORTANT: Execute every step in order, top to bottom.

### Step 1: Remove the file from the working tree and index
- Run `git rm app_docs/feature-d77654ec-sync-openapi-freshness.md` from the repo/worktree root
- This stages the deletion for the next commit

### Step 2: Commit the deletion
- Create a new commit: `chore: remove out-of-scope feature doc from sync PR`
- The commit message should note this file was generated by sdlc_documenter but falls outside the sync/generate script output scope

## Key Decisions & Rationale
**Lines of code to change:** 0 added, 75 deleted (file removal only)
**Risk level:** low
**Testing required:** Confirm `app_docs/feature-d77654ec-sync-openapi-freshness.md` no longer appears in `git diff main..HEAD --name-only`

## Verification
Execute every command to validate the patch is complete with zero regressions.

- `git diff main..HEAD --name-only` — confirm `app_docs/feature-d77654ec-sync-openapi-freshness.md` is absent from the diff
- `git status` — confirm working tree is clean
- The remaining diff should contain only: `api-reference/endpoint/*.mdx` stubs, `docs.json`, `scripts/generate-api-reference.mjs` (and `api-reference/openapi.json` if present) — all legitimate sync/generate outputs

## Known Limitations / Follow-ups
The sdlc_documenter commit (`f6163d8`) added only this one file. Removing the file effectively makes that commit a no-op; the new deletion commit supersedes it. No other files from that commit need to be touched.
