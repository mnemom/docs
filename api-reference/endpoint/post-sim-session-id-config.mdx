---
title: "Update a running Dojo sim session's live config (mode / security posture / judge)."
description: "Lets the operator change three run controls WHILE a sim is actively running (previously fixed at Begin-Sim): mode (auto/manual), security_posture_in_prompt (..."
openapi: "POST /sim/{session_id}/config"
---
