---
title: "Get a single outbound (egress) evaluation"
description: "Authorization: the caller must be authenticated and resolve to an organization, and evaluation_id is queried scoped to that org's own agents, so an evaluatio..."
openapi: "GET /safe-house/outbound/evaluations/{evaluation_id}"
---
