---
title: "Get the signed-in user's own per-turn µ spend (D5 per-user visibility)"
description: "Returns the CURRENT user's own per-turn spend, self-scoped by session sub only — there is no person_id parameter and a user can never read another person's t..."
openapi: "GET /billing/usage/per-turn"
---
