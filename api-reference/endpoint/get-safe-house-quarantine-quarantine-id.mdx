---
title: "Get a single quarantined message"
description: "Authorization: the caller must be authenticated and resolve to an organization, and quarantine_id is queried scoped to that org's own agents, so a record own..."
openapi: "GET /safe-house/quarantine/{quarantine_id}"
---
