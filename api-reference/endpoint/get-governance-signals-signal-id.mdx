---
title: "Get a single governance signal by ID"
description: "Authorization: the signal's owning organization is loaded from the row itself and the caller must be a member of that org, so a signal_id belonging to anothe..."
openapi: "GET /governance/signals/{signal_id}"
---
