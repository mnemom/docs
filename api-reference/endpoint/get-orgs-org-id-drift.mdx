---
title: "Paginated drift alerts for an org fleet"
description: "Returns active and recent drift alerts for every agent in the org, plus a summary block (total_active, agents_drifting, high_severity, trend)."
openapi: "GET /orgs/{org_id}/drift"
---
