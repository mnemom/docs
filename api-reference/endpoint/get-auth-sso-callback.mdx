---
title: "IdP callback landing — issues session and redirects to SPA"
description: "Validates the HMAC-signed state (from the mnemom_sso_initiator cookie), consumes the single-use nonce, exchanges the PKCE code, enforces domain + org binding..."
openapi: "GET /auth/sso/callback"
---
