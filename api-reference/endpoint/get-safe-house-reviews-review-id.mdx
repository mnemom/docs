---
title: "Get a single review-hold lifecycle row by id"
description: "Agent-scoped (Task A2, canonical-consumer plan): returns 404, never 403, for a review_id belonging to another agent — the lookup is always scoped to the call..."
openapi: "GET /safe-house/reviews/{review_id}"
---
