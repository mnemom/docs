---
title: "Mint an ephemeral compute/operate key (gateway + api:read + api:write)."
description: "Mints a Mnemom-provided, ephemeral API key carrying gateway + api:read + api:write (the standard-key default; no admin scopes), hung off the dedicated dojo-s..."
openapi: "POST /dojo/compute-keys"
---
