---
title: "Begin an SSO login flow"
description: "Looks up the SSO provider for the email's domain, generates PKCE + HMAC-signed state, sets a short-lived mnemom_sso_initiator cookie, and returns the IdP URL..."
openapi: "POST /auth/sso/initiate"
---
