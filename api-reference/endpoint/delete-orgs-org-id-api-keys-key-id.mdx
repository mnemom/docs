---
title: "Revoke organization API key"
description: "Authorization: the caller must be a member of org_id (owner/admin, or the member who created the key), and key_id is matched together with org_id, so a key o..."
openapi: "DELETE /orgs/{org_id}/api-keys/{key_id}"
---
