---
title: "List all revisions of a posture"
description: "Authorization: an org-scoped posture is readable only by a member (any role) of the organization that owns it — the org is loaded from the posture row, never..."
openapi: "GET /postures/{posture_id}/revisions"
---
