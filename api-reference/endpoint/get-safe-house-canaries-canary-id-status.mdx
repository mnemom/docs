---
title: "Check whether a canary has been triggered"
description: "Authorization: the caller must be authenticated and resolve to an organization, and canary_id is queried scoped to that org's own agents, so a canary owned b..."
openapi: "GET /safe-house/canaries/{canary_id}/status"
---
