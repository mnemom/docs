---
title: "Dismiss a governance signal as not actionable"
description: "Authorization: the signal's owning organization is loaded from the row itself and the caller must be an owner/admin of that org, so a signal_id belonging to ..."
openapi: "POST /governance/signals/{signal_id}/dismiss"
---
