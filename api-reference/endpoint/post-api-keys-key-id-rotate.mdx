---
title: "Rotate a personal API key"
description: "Atomically mints a replacement key with the same name and scopes as the original, then immediately revokes the old key."
openapi: "POST /api-keys/{key_id}/rotate"
---
