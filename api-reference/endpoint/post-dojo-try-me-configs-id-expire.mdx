---
title: "Burn/expire a Try-Me Config, freeing its token."
description: "Sets uses_remaining to 0 and stamps expires_at, immediately disabling the token and freeing it for re-mint."
openapi: "POST /dojo/try-me-configs/{id}/expire"
---
