---
title: "Re-arm a Try-Me Config, making its token resolvable again."
description: "The inverse of expire: re-arms uses_remaining to usage_limit and clears the burn state (used_at, expires_at, briefing_manifest_ref) so the next resolve mints..."
openapi: "POST /dojo/try-me-configs/{id}/reset"
---
