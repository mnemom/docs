---
title: "Revoke an API key"
description: "Authorization: key_id is matched together with the authenticated caller's user id, so a key owned by another user is invisible (404) and no one else's person..."
openapi: "DELETE /api-keys/{key_id}"
---
