---
title: "Agent Smith promotion-run audit feed (admin console)."
description: "Returns the raw promotion-gate audit trail (newest first) for the /admin/agent-smith Promotion-runs table: one row per candidate evaluation with the gate's p..."
openapi: "GET /smith/promotion-runs"
---
