---
title: "Remove AIP webhook"
description: "Authorization: the registration is resolved to its parent agent and the caller must be a member of the organization that owns that agent; a registration unde..."
openapi: "DELETE /aip/webhooks/{registration_id}"
---
